# Test: Bridge WETH from Base to Arbitrum via Across (for gas)

spell TestAcrossWethBaseToArb

  version: "1.0.0"
  description: "Bridge WETH from Base to Arbitrum via Across — for gas"
  assets: [WETH]

  params:
    amount: 3000000000000000
    destination_chain: 42161

  venues:
    across: @across

  on manual:
    across.bridge(WETH, params.amount, params.destination_chain)
    emit bridged(asset=WETH, direction="base_to_arb", amount=params.amount)
